
APP_NAME=Laravel
APP_ENV=testing
APP_KEY=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000

# Database Configuration (Matches GitHub Actions MySQL Service)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel_test
DB_USERNAME=root
DB_PASSWORD=root

# Queue & Cache
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file

# Mail (Disable emails in CI)
MAIL_MAILER=log

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug

# Testing Variables (If Needed)
TEST_API_TOKEN=your-test-token
